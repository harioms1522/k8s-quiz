// Quiz data bundled with the component - no need to copy files!
// Imported directly from the JSON file and bundled with the package

import quizDataJson from './quiz_game_data.json';

export default quizDataJson;
